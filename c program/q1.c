#include <stdio.h>

int main() {
    int item;
    int product;
    if(item!= 0)
    {
        product= product*item;
        printf("Product: %d\n", product);
    }
    else
    {
        printf("Product: %d\n", product);
    }
    return 0;
}